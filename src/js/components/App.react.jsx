'use strict';


var React = require( 'react' );
var Marty = require( 'marty' );

var App = React.createClass({
    render: function() {
        return (
            <div>Hello, world</div>
        );
    }
});


module.exports = App;
